apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - vcluster-custom-resource.yaml

namespace: cattle-system # we use this ns since Secret/rancher-tls created by cert-manager is already here in host (management) cluster
