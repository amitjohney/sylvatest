apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
- target:
    kind: Certificate
  patch: |
    - op: add
      path: /spec/emailAddresses/-
      value: added-by-component-1-used-in-base@test.com
