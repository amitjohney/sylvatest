kind: GitRepo
apiVersion: fleet.cattle.io/v1alpha1
metadata:
  name: monitoring
  namespace: fleet-local
spec:
  repo: ${GIT_REPO}
  branch: ${BRANCH}
  paths:
    - monitoring-crd/
    - monitoring/
  targets:
    - clusterName: local
