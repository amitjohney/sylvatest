apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ksm-kyverno.yaml 

configMapGenerator:
- name: 'flux-custom-resource-state-cm'
  namespace: sylva-system
  options:
    disableNameSuffixHash: true
  files:
  - config.yaml=ksm-config.yaml

