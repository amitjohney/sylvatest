apiVersion: v1
kind: ConfigMap
metadata:
  name: mgmt-cluster-settings
  namespace: default
data:
  clouds_yaml: ${CLOUDS_YAML}
  image: ${IMAGE}
  worker_machine_image: ${WORKER_MACHINE_IMAGE}
  capo:
    network_id: ${CAPO_NETWORK_ID}

