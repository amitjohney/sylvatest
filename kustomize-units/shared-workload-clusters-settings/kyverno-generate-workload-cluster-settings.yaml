apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: shared-workload-clusters-setttings
spec:
  rules:
  - name: generate-cm
    match:
      any:
      - resources:
          kinds:
          - Namespace
          selector:
            matchLabels:
              app.kubernetes.io/name: shared-workload-cluster
    generate:
      kind: ConfigMap
      apiVersion: v1
      name: shared-workload-clusters-settings
      namespace: "{{request.object.metadata.name}}"
      synchronize: true
      clone:
        name: shared-workload-clusters-settings
        namespace: default
