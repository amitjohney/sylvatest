
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../kube-cronjob

configMapGenerator:
- name: '${JOB_NAME}-${JOB_TARGET_NAMESPACE:-sylva-system}-cm'
  namespace: kube-job
  options:
    disableNameSuffixHash: true
  files:
  - kube-job.sh=cluster-creator-renew.sh
