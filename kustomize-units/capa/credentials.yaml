---
apiVersion: v1
kind: Secret
metadata:
  name: capa-manager-bootstrap-credentials
  namespace: capa-system
data:
  #credentials: ${CREDENTIALS} 
  credentials: {{ .Values.CREDENTIALS.credentials | b64enc }}