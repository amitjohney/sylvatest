---
apiVersion: v1
kind: Secret
metadata:
  name: capa-manager-bootstrap-credentials
  namespace: capa-system
data:
  #credentials: ${CREDENTIALS} 
  credentials: ${CREDENTIALS}["credentails"] | b64enc
  #credentials: {{ CREDENTIALS.credentials | b64enc }}