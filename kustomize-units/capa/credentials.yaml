---
apiVersion: v1
kind: Secret
metadata:
  name: capa-manager-bootstrap-credentials
  namespace: capa-system
data:
  #credentials: ${CREDENTIALS} 
  credentials: {{ .Values.kustomization_substitute_secrets.CREDENTIALS | b64enc | quote }}
  #credentials: {{ CREDENTIALS.credentials | b64enc }}