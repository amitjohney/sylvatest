apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
- target:
    kind: RKE2ControlPlane
  patch: |
    - op: add
      path: /spec/rke2ConfigSpec/files/-
      value:
        path: /etc/resolv.conf
        owner: root
        permissions: "0644"
        content: |
          nameserver ${K8SGATEWAY_LB_IP}
