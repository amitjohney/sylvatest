apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
- target:
    kind: OpenStackMachineTemplate
    labelSelector: role=worker
  patch: |
    - op: add
      path: /spec/template/spec/securityGroups/-
      value:
        name: ${WORKER_SECURITY_GROUP_NAME}-ironic
