---

units:
  cluster:
    kustomization_spec:
      path: ./kustomize-units/cluster-manifests/kubeadm-capo/orange-falcon
  workload-cluster:
    repo: sylva-core
    kustomization_spec:
      path: ./kustomize-units/cluster-manifests/kubeadm-capo/dns-client-orange-falcon
      postBuild:
        substitute:
          CAPO_NETWORK_ID: '{{ .Values.cluster.capo.network_id }}'
          CLUSTER_EXTERNAL_IP: # set an IP for your workload cluster kube-api LB VIP here
          FLAVOR_NAME: '{{ .Values.cluster.capo.flavor_name }}'
          SSH_KEY_NAME: '{{ .Values.cluster.capo.ssh_key_name }}'

cluster:
  image: capo-ubuntu-2004-kube-v1.23.6-calico-3.23.1
  capi_providers:
    infra_provider: capo
    bootstrap_provider: cabpk
  capo:
    ssh_key_name: # put the name of your nova SSH keypair here
    network_id: c314d52c-80fe-42b6-9092-55be383d1951
    ## If cinder CSI is used, provide a name for the storage class and the storage type from OpenStack
    storageClass:
      name: "cinder-ceph-ssd"
      type: "ceph_ssd"

  cluster_external_ip: 1.2.3.4  # replace me

  test_workload_cluster_name: "first-workload-cluster"

proxies:
  # put your own proxy settings here if you need
  http_proxy: ""
  https_proxy: ""
  no_proxy: ""

# add your local docker.io registry mirror to avoid rate limitting if required
dockerio_registry_mirror: ""
