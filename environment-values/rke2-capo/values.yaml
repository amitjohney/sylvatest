debug: true

components:
  cluster:
    kustomization_spec:
      path: kustomize-components/cluster-manifests/rke2-capo/orange-falcon
  rancher:
    enabled: management-only
  k8s-gateway:
    enabled: management-only
  capi-rancher-import:
    enabled: management-only

cluster:
  image: Ubuntu 20.04 Pack
  flavor:
    infra_provider: capo
    bootstrap_provider: cabpr
  capo:
    ssh_key_name: telco
    network_id: c314d52c-80fe-42b6-9092-55be383d1951
    kube_vip_ip: 1.2.3.4  # replace me

proxies:
  # put your own proxy settings here if you need
  http_proxy:
  https_proxy:
  no_proxy:
