cluster:
  capo:
    clouds_yaml:
      clouds:
        capo_cloud:
          auth:
            auth_url: # replace me
            user_domain_name: # replace me
            project_domain_name: # replace me
            project_name: # replace me
            username: # replace me
            password: # replace me
          region_name: # replace me
          verify: false
# Add external ceph cluster details
ceph:
  cephfs_csi:
    adminID: "" # ceph user allowed to use ceph file system
    adminKey: "" # ceph keyring for the ceph user
