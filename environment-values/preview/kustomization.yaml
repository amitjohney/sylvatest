apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
- preview-namespace.yaml

namespace: telco-cloud-init-preview

patches:
- path: helm-release.yaml
  target:
    kind: HelmRelease
