---

units:
  cluster:
    kustomization_spec:
      path: ./kustomize-units/cluster-manifests/kubeadm-capd/base
  workload-cluster:
    kustomization_spec:
      path: ./kustomize-units/cluster-manifests/kubeadm-capd/base

cluster:
  capi_providers:
    infra_provider: capd
    bootstrap_provider: cabpk
  capd:
    docker_host: unix:///var/run/docker.sock  # dynamically replaced in CI

  cluster_external_ip: # use the .100 (or any other that wouldn't be assigned by Docker) IP from the local kind subnet, returned by: docker network inspect -f '{{json .}}' kind | jq -r .IPAM.Config[0].Subnet

proxies:
  # put your own proxy settings here if you need
  http_proxy: ""
  https_proxy: ""
  no_proxy: ""

# add your local docker.io registry mirror to avoid rate limiting if required
dockerio_registry_mirror: ""
