cluster:
  baremetal_host_default:
    credentials:
      username: admin
      password: bmcpass

units:
  workload-cluster:
    helmrelease_spec:
      values:
      cluster:
        baremetal_host_default:
          credentials:
            username: admin
            password: bmcpass
