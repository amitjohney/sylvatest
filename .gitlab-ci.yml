---
stages:
  - test

test:
  stage: test
  image: docker:24.0.7
  services:
    - docker:24.0.7-dind
  variables:
    DOCKER_HOST: tcp://docker:2375/
    DOCKER_TLS_CERTDIR: ""
  script:
    - date
    - echo 'hi'
    - touch new.yaml
    - sleep 1000
  tags:
    - sylva-ci-rocket