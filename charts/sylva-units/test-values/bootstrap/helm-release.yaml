
# this values file is only here to remove keys that
# contain templating statements using Helm "lookup" that
# can only work when interpreted on a real bootstrap cluster

units:
  sylva-units:
    helmrelease_spec:
      valuesFrom:
      values:
