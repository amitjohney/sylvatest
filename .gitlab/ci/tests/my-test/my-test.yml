---

.my_test:
    - echo -e "ok"

print-something:
    stage: test
    rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event' ||  $CI_PIPELINE_SOURCE == 'pipeline'
      changes:
            - .gitlab/ci/**/*
    script:
        - echo "Running my_test..."
        - !reference [.my_test]

