---

.my_test:
    - echo -e "ok"

test-after-deployment:
  stage: deployment-test
  extends:
    - .test-tags
  script:
    - echo "Running my_test..."
    - !reference [.my_test]
  rules:
    - when: never
    
